# supermarket-price-tracker

Once a month: search current supermarket prices for the basket in `config.json`,
write them to `prices.json`, then commit and push.

`index.html` reads `prices.json` and shows the interactive ranking.
